apiVersion: v1
kind: Service
metadata:
  name: practica-frontend
  namespace: devops-lab
spec:
  type: ClusterIP
  selector:
    app: practica-frontend
  ports:
    - name: http
      port: 5374        
      targetPort: 80     